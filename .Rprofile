RENV_PATHS_LIBRARY_ROOT <- "./renv"
RENV_PATHS_CACHE <- "./renv/.cache"
RENV_PATHS_LIBRARY <- "renv/library"
RENV_PATHS_LOCK <- "renv.lock"
RENV_PATHS_SETTINGS <- "renv/settings.json"
RENV_PATHS_LIBRARY_ROOT_ASIS <- TRUE
RENV_CONFIG_SANDBOX_ENABLED <- FALSE
source("renv/activate.R")
# Dev-Dependencies
if(!require(remotes)) renv::install("remotes")
if(!require(roxygen2md)) renv::install("roxygen2md")
if(!require(rmarkdown)) renv::install("rmarkdown")
if(!require(covr)) renv::install("covr")
if(!require(DT)) renv::install("DT")
if(!require(htmltools)) renv::install("htmltools")

